<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:text="Attivita"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/nome_attivita"
        android:textSize="25sp"
        android:textColor="@color/black"
        android:layout_alignParentStart="true"
        android:layout_marginStart="5dp"
        android:layout_alignParentLeft="true"
        android:layout_marginLeft="5dp" />

    <Button
        android:text="Stop"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/stop_button"
        android:textSize="20sp"
        android:background="@drawable/button_selector"
        style="@style/SmallButtonStyle"
        android:layout_marginRight="5dp"
        android:layout_marginBottom="2dp"
        android:layout_below="@+id/nome_attivita"
        android:layout_toLeftOf="@+id/cancel_button"
        android:layout_toStartOf="@+id/cancel_button" />

    <ProgressBar
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/attivita_progressbar"
        android:layout_below="@+id/nome_attivita"
        android:layout_alignLeft="@+id/nome_attivita"
        android:layout_alignStart="@+id/nome_attivita"
        android:layout_marginLeft="7dp"
        android:layout_marginStart="7dp" />

    <Button
        android:text="canc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/cancel_button"
        android:textSize="20sp"
        android:background="@drawable/button_selector"
        style="@style/SmallButtonStyle"
        android:layout_marginRight="5dp"
        android:layout_marginBottom="2dp"
        android:layout_alignBaseline="@+id/stop_button"
        android:layout_alignBottom="@+id/stop_button"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

</RelativeLayout>